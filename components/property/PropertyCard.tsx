import React from 'react'

type PropertyCardProps = {
  key: string;
  property: any;
}

const PropertyCard = ({key, property}: PropertyCardProps) => {
  return (
    <div>
       hany
    </div>
  )
}

export default PropertyCard
